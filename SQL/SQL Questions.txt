=============================================================
SQL PRACTICE HANDBOOK - BASIC TO MEDIUM
=============================================================
Tables: users, addresses, admin_users, user_log
Total Questions: 64
=============================================================


=============================================================
TOPIC 1 - SELECT BASICS
=============================================================

Q1  [BASIC]  Select all columns from the users table.

Q2  [BASIC]  Select only the name and email columns from users.

Q3  [BASIC]  Select the name and salary of all users, and alias
             salary as monthly_salary.

Q4  [BASIC]  Fetch distinct gender values from the users table.

Q5  [BASIC]  Select the top 5 users ordered by salary descending.

Q6  [MEDIUM] Select all users created after January 1, 2024.

Q7  [MEDIUM] Select users whose salary is between 30000 and 70000.

Q8  [MEDIUM] Select users whose name starts with the letter 'A'.

Q9  [MEDIUM] Select all users where email ends with '@gmail.com'.

Q10 [MEDIUM] Select name and city from users joined with addresses,
             for users in the city 'Pune'.


=============================================================
TOPIC 2 - WHERE & FILTERS
=============================================================

Q11 [BASIC]  Fetch all Female users from the users table.

Q12 [BASIC]  Fetch users where salary is NULL.

Q13 [BASIC]  Fetch users where salary is NOT NULL.

Q14 [BASIC]  Fetch users with gender 'Male' AND salary greater
             than 50000.

Q15 [BASIC]  Fetch users with city 'Mumbai' OR city 'Delhi'
             from addresses.

Q16 [MEDIUM] Fetch users whose pincode is in
             ('411001', '400001', '110001').

Q17 [MEDIUM] Fetch users whose name contains the word 'kumar'
             (case-insensitive).

Q18 [MEDIUM] Fetch all users who do NOT have an address record.

Q19 [MEDIUM] Fetch users born between 1990-01-01 and 2000-12-31.

Q20 [MEDIUM] Fetch users whose name is exactly 5 characters long.


=============================================================
TOPIC 3 - AGGREGATIONS
=============================================================

Q21 [BASIC]  Count the total number of users in the users table.

Q22 [BASIC]  Find the maximum salary among all users.

Q23 [BASIC]  Find the minimum salary among all users.

Q24 [BASIC]  Calculate the average salary of all users.

Q25 [BASIC]  Find the total sum of salaries for all Male users.

Q26 [MEDIUM] Count the number of users grouped by gender.

Q27 [MEDIUM] Find the average salary per city
             (join users and addresses).

Q28 [MEDIUM] Count users grouped by state, but only show states
             with more than 3 users.

Q29 [MEDIUM] Find the gender group with the highest
             average salary.

Q30 [MEDIUM] Count how many users were created each month
             (group by month of created_at).


=============================================================
TOPIC 4 - JOINs
=============================================================

Q31 [BASIC]  Write an INNER JOIN between users and addresses
             to get user name and their city.

Q32 [BASIC]  Write a LEFT JOIN to get all users along with their
             address (show NULL if no address exists).

Q33 [BASIC]  Find all users who do NOT have an entry in the
             addresses table using LEFT JOIN.

Q34 [MEDIUM] Join users and user_log to find the name and
             login count for each user.

Q35 [MEDIUM] Find users who exist in both users and admin_users
             tables (match by email).

Q36 [MEDIUM] List all user names with their full address
             (street, city, state, pincode).

Q37 [MEDIUM] Count the number of addresses each user has,
             including users with zero addresses.

Q38 [MEDIUM] Write a self-join on users to find pairs of users
             born in the same year.


=============================================================
TOPIC 5 - SET OPERATIONS
=============================================================

Q39 [BASIC]  Use UNION to combine name and email from
             users and admin_users.

Q40 [BASIC]  Use UNION ALL to combine users and admin_users
             without removing duplicates.

Q41 [MEDIUM] Find emails that exist in both users and admin_users
             using INTERSECT (or equivalent with IN/JOIN).

Q42 [MEDIUM] Find emails in users that are NOT present in
             admin_users using EXCEPT or NOT IN.

Q43 [MEDIUM] Use UNION to get a combined list of all unique
             cities from addresses.


=============================================================
TOPIC 6 - SUBQUERIES
=============================================================

Q44 [BASIC]  Find users whose salary is above the average
             salary using a subquery.

Q45 [BASIC]  Find the user with the maximum salary
             using a subquery.

Q46 [MEDIUM] Fetch all users who have at least one address
             using EXISTS.

Q47 [MEDIUM] Fetch all users who have NO address
             using NOT EXISTS.

Q48 [MEDIUM] Find users whose salary is greater than ALL
             Female users' salaries.

Q49 [MEDIUM] Find users whose salary is greater than ANY
             Male user's salary.

Q50 [MEDIUM] Use a subquery in the FROM clause to get average
             salary per city, then filter cities where avg > 50000.


=============================================================
TOPIC 7 - DDL / DML
=============================================================

Q51 [BASIC]  Insert a new user into the users table
             with all fields filled.

Q52 [BASIC]  Insert a new address linked to user_id = 1.

Q53 [BASIC]  Update the salary of user with id = 3 to 75000.

Q54 [BASIC]  Delete all records from user_log where user_id = 5.

Q55 [MEDIUM] Add a new column phone VARCHAR(15) to the
             users table.

Q56 [MEDIUM] Rename the column street to address_line
             in the addresses table.

Q57 [MEDIUM] Add a UNIQUE constraint on the email column
             of admin_users.

Q58 [MEDIUM] Increase the salary of all users in city 'Pune'
             by 10% (use JOIN in UPDATE).


=============================================================
TOPIC 8 - WINDOW FUNCTIONS
=============================================================

Q59 [BASIC]  Use ROW_NUMBER() to assign row numbers to users
             ordered by salary descending.

Q60 [MEDIUM] Use RANK() to rank users by salary within each
             gender group using PARTITION BY.

Q61 [MEDIUM] Use DENSE_RANK() to rank users by salary and
             show only the top 3 ranks.

Q62 [MEDIUM] Use LAG() to show each user's salary and the
             previous user's salary (ordered by id).

Q63 [MEDIUM] Use SUM() OVER (PARTITION BY gender) to show each
             user's salary and gender-wise total salary
             side by side.

Q64 [MEDIUM] Use NTILE(4) to divide users into
             salary quartiles.


=============================================================
PROGRESS TRACKER
=============================================================

Topic                  Total   Done
-----------------------+-------+-----
SELECT Basics          | 10    |
WHERE & Filters        | 10    |
Aggregations           | 10    |
JOINs                  |  8    |
Set Operations         |  5    |
Subqueries             |  7    |
DDL / DML              |  8    |
Window Functions       |  6    |
-----------------------+-------+-----
TOTAL                  | 64    |

=============================================================
NOTE: All questions use these tables:
  - users
  - addresses
  - admin_users
  - user_log
=============================================================
